# 变更记录

用于记录本项目每次功能和界面调整，方便之后回看改了什么、为什么改。

变更记录按倒序展示：最新日期在上，同一天内最新时间在上。
同一天如果多次修改，按小时级别拆分记录，格式为 `### HH:mm`。

## 2026-06-12

### 09:03

#### 上移侧边栏导航

- 将侧边栏导航从垂直居中改为偏上对齐，减少顶部无效留白。
- 保持侧边栏 240px 宽度和当前视觉风格不变。

### 08:35

#### 优化页面布局比例

- 左侧侧边栏固定为 240px，并降低 active 导航按钮视觉权重。
- 主内容区调整为 960px 最大宽度并居中展示。
- 输入区高度调整为 140px，发送按钮固定在右下角。
- 笔记卡片最小高度调整为 120px，卡片间距调整为 16px，内容 padding 调整为 24px。

## 2026-06-11

### 15:03

#### 取消输入框自动聚焦

- 移除输入框页面加载时的 `autofocus`。
- 保存或取消编辑后不再自动把焦点拉回输入框，避免页面中间一直显示输入光标。

### 14:42

#### 调整 CLAUDE.md 为通用模板

- 移除 `CLAUDE.md` 中绑定当前项目的固定路径说明。
- 增加通用的工作区确认步骤：先执行 `pwd` 和 `git status --short`。
- 保留 checkpoint、变更记录同步、回滚说明、文档同步等可复用协作规则。
- 该文件之后可以复制到其他项目根目录复用。

### 14:38

#### checkpoint: 增加 Excel 导出

- commit: `2661b8f`
- 说明：该 commit 实际包含今天已完成的整批改动，包括 Excel 导出、图片导出优化、flomo 风格界面、变更记录和回滚文档。
- 状态：本地已提交，尚未推送 GitHub。
- 注意：因为这些改动是在一次 `git add .` 后统一提交的，不能用这个 commit 区分“导出版 / 图片导出版 / flomo 版”三个中间版本。

#### 新增 CLAUDE.md 协作规则

- 在项目根目录新增 `CLAUDE.md`。
- 约定以后每完成一个独立版本，先提醒用户确认是否要做本地 checkpoint。
- 用户确认后创建本地 commit，并把 commit id 记录到本文件。

### 14:27

#### 调整变更记录粒度

- 同一天多次修改时，在日期下按 `HH:mm` 记录每次变更。
- 已有但未记录具体时间的内容，统一放入 `时间未记录` 分组。

### 时间未记录

#### 增加 Excel 导出

- 在页面中增加一键导出入口。
- 导出字段为：分类、日期、内容。
- 导出范围为全部已收集内容，不受页面搜索或标签筛选影响。
- 导出文件名格式为 `碎片导出-YYYYMMDD.xls`。

#### 优化图片导出

- 内容里包含图片时，导出文件的内容列会显示 `[图片]` 标记。
- 图片会以预览图形式嵌入导出的 Excel 可打开文件中。
- 保留文字和图片同时存在的记录展示。

#### 参照 flomo 调整主界面

- 将整体界面调整为暗色侧边栏 + 主内容区布局。
- 左侧增加全部笔记、导出 Excel、全部标签入口。
- 主区包含顶部搜索、输入框和笔记卡片流。
- 从内容中的 `#标签` 自动识别标签，并展示在每条记录内容前。
- 每条记录右侧增加 `...` 操作入口，点击后可编辑或删除。
- 保留原有本地存储、搜索、编辑、删除和图片粘贴能力。

#### 项目路径调整

- 当前本地项目路径改为：`/Users/kityhello/workplace/project/pages_shouji`。
- 旧路径 `/Users/kityhello/Documents/codex_total/pages_shouji` 已不再作为当前工作区使用。

#### 建立回滚机制

- 新增 `docs/rollback-guide.md`，记录未提交、已提交未推送、已推送三种情况下的回滚方式。
- 之后每次较大改动，都在本文件记录修改前的回滚点 commit 和建议回滚方式。
- 当前这批改动尚未提交，回滚点是最近提交 `a737f3b Redesign homepage with Swiss style`。
